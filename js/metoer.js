//this will also have to spawn the meteor
function Meteor(boardHeight,boardWidth){
	this.boardHeight = boardHeight;
	this.boardWidth = boardWidth;
	//this.initBoardListener();
	//console.log(this.gameBoard);
	//this.setPosition();
}